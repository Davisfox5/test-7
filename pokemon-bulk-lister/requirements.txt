opencv-python>=4.9.0
numpy>=1.26.0
requests>=2.31.0
pandas>=2.2.0
python-dotenv>=1.0.0
cloudinary>=1.39.0
Pillow>=10.2.0
Flask>=3.0.0
anthropic>=0.70.0
SQLAlchemy>=2.0.0

# Optional: marketplace card payments via Stripe Checkout (set STRIPE_SECRET_KEY)
# stripe>=7.0.0

# Optional: only needed if you turn on Terapeak headless scraping
# (--terapeak or ENABLE_TERAPEAK=1). After install, run:
#   playwright install chromium
playwright>=1.40.0
